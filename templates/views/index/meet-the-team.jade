.meet-the-team.content-max-width
  .mtt-header
    each person, ix in team
      .mtt-box(id=person.id, ix=ix)
        img.img-max-width-100(src=person.heroImage.secure_url)
    .mtt-box-sel.hidden
  .pull-left(id="personContent")
    .person-title
      .person-name
      .person-role
    .person-description